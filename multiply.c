#include <stdio.h>
int main()
{
    float a, b ;
    printf("value of a");
    scanf("%f", &a);

    printf("value of b");
    scanf("%f", &b);

    printf("the value of multiplication is %f",a*b);

    return 0;
}